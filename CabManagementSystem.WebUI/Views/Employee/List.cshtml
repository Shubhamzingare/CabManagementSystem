@model CabManagementSystem.WebUI.Models.EmployeeListViewModel

@{
    ViewBag.Title = "Employees";
    Layout = "~/Views/Shared/_DashboardLayout.cshtml";
}

<div class="panel panel-default">
    <div class="panel-body">
        <table class="table  table-hover w-auto" style="color:white; font-weight:100; font-family:'Times New Roman', Times, serif" >
            <thead>
                <tr>
                    <td colspan="13" align="right">
                            <h2>Available Employees</h2>
                    </td>
                </tr>
                <tr class="table-dark">
                    <th>ID</th>
                    <th>Name</th>
                    <th>Permanent Address</th>
                    <th>Communication Address</th>
                    <th>Qualifications</th>
                    <th>Date Of Birth</th>
                    <th>Vehicle required (Yes/No)</th>
                    <th>Gender</th>
                    <th>Phone Number</th>
                    <th>Designation</th>
                    <th>Department</th>
                    <th>Date of Joining</th>
                    <th style="width: 25%">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var p in Model.Employees)

                {
                    @Html.Partial("EmployeeSummery", p)
                }
                <tr>
                    <td colspan="13" align="right">

                        @Html.PageLinks(Model.PagingInfo, x => Url.Action("List", new { page = x }))
                    </td>
                </tr>
            </tbody>

        </table>
    </div>
</div>
